import React from 'react';import s from './actionBar.module.css'import {useDispatch, useSelector} from "react-redux";import {AppRootStateType} from "../store/store";import {resCounterValueAC} from "../store/counter-reducer";type ActionBarType = {    counter: number    increment: () => void}export const ActionBar = (props: ActionBarType) => {    const minValue = useSelector<AppRootStateType, number>(state => state.counter.minValue)    const maxValue = useSelector<AppRootStateType, number>(state => state.counter.maxValue)    const isEdit = useSelector<AppRootStateType, boolean>(state => state.counter.isEdit)    const dispatch = useDispatch()    const resHandler = () => {        dispatch(resCounterValueAC(minValue))    }    return (        <div>            {                isEdit ? (<div className={s.actionBar}>                    <button onClick={props.increment} disabled={props.counter >= maxValue ? true : false}>                        inc                    </button>                    <button onClick={resHandler} disabled={props.counter > maxValue ? true : false}>                        reset                    </button>                </div>) : ''            }        </div>    )}